name: Linux build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build-linux:
    runs-on: ubuntu-24.04

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y \
            build-essential \
            cmake \
            git \
            pkg-config \
            libopenblas-dev \
            liblapack-dev \
            libopencv-dev \
            libtesseract-dev \
            libleptonica-dev \
            libcurl4-openssl-dev \
            libglfw3-dev \
            libgl1-mesa-dev \
            libx11-dev \
            libxext-dev

      - name: Cache CMake dependencies
        uses: actions/cache@v4
        with:
          path: build/_deps
          key: cmake-deps-${{ runner.os }}-${{ hashFiles('CMakeLists.txt', 'cmake/DependenciesLinux.cmake') }}
          restore-keys: |
            cmake-deps-${{ runner.os }}-

      - name: Configure
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --parallel

      - name: Package binary
        run: |
          mkdir -p dist
          BIN=$(find build -maxdepth 2 -type f -executable ! -name "*.so" | head -n 1)
          echo "Found binary: $BIN"
          cp "$BIN" dist/RuneHelper
          cp RuneHelper/resources/combinations.json dist/combinations.json
          tar -czf runehelper-linux-x86_64.tar.gz -C dist RuneHelper combinations.json

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: runehelper-linux-x86_64
          path: runehelper-linux-x86_64.tar.gz
